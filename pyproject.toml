[project]
name = "tooling"
version = "0.0.0"
description = "Tooling repository root. It holds no application code."
requires-python = ">=3.12"

[tool.uv]
package = false

[dependency-groups]
dev = [
    "ruff>=0.12",
]
